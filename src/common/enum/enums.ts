
export enum StatusEnum {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum StatusProjectEnum {
  PENDING = 'pending',
  DONE = 'done',
  ON_PROGRESS = 'on_progress',
}


export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}
